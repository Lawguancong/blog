import axios from 'axios'


const initState = {

}
//reducer -> user
export function user(state = initState,action) {
    switch(action.type){

        default:
            return state
    }
}
